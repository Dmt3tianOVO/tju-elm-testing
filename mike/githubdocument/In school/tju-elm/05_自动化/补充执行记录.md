# 补充执行记录（成员C：自动化两轮 + 登录判定双分支观察）

- 执行者：成员 C（演示机，账号 admin）　日期：2026-09-15
- 仓库基线：`8efcb99`（`tju-elm`，当前 HEAD 即此基线，工作区仅新增 `05_自动化/` 交付物）
- 前端 `http://localhost:8084`、后端 `http://localhost:8080`，CORS 已由后端 `WebMvcConfig` 放行
- 运行环境：Windows Server 2022 / Python 3.12.7 / pytest 8.3.4 / selenium 4.27.1 / Chrome 128.0.6613.138 / ChromeDriver 128.0.6613.138
- 统一命令：项目根目录 `python -m pytest 05_自动化/test_smoke.py -v`（两轮各收集到 **2 个测试**，各自独立临时浏览器配置目录，干净会话）

> 组内正式位置为 `04_测试执行/补充执行记录.md`；本项目仓库中暂无该目录，先以本文件落证据，合并报告时整体搬入，不新增 TC-11/12 主用例。

## 1. R1 / R2 自动化执行结果

| 轮次 | 复用用例 | 测试函数 | 结果 | 关键断言实测 | 耗时 | 证据 |
|---|---|---|---|---|---|---|
| R1 | TC-01 / TC_login_func_001 | test_tc01_login_jump_and_session | PASSED | 未登录态真实登录后 URL 含 `/index`；`sessionStorage.user` 解析 `userId=19112121212` | 合计 13.38s，2 passed | evidence/R1/console_R1.txt；test_tc01_login_jump_and_session.png（停在 /index） |
| R1 | TC-03 / TC_business_func_003 | test_tc03_business_food_name_and_price | PASSED | 列表含 10001「万家饺子（软件园E18店）」→ 详情 `?businessId=10001`，h1 一致；同 li 内「纯肉鲜肉（水饺）」价格数值 = 16.00（页面渲染 ¥16） | 同上 | evidence/R1/console_R1.txt；test_tc03_business_food_name_and_price.png（停在商家详情） |
| R2 | TC-01 / TC_login_func_001 | test_tc01_login_jump_and_session | PASSED | 同 R1（全新临时 user-data-dir，会话不共享） | 合计 13.17s，2 passed | evidence/R2/console_R2.txt；test_tc01_login_jump_and_session.png |
| R2 | TC-03 / TC_business_func_003 | test_tc03_business_food_name_and_price | PASSED | 同 R1 | 同上 | evidence/R2/console_R2.txt；test_tc03_business_food_name_and_price.png |

命令（R2 仅 RUN_TAG 不同）：

```powershell
$env:CHROMEDRIVER_PATH="C:\Users\admin\.local\drivers\chromedriver-win64\chromedriver.exe"
$env:RUN_TAG="R1"; python -m pytest 05_自动化/test_smoke.py -v
$env:RUN_TAG="R2"; python -m pytest 05_自动化/test_smoke.py -v
```

两轮结果一致（各 2 passed）。失败截图机制保留在脚本 fixture 中；本机两轮无失败，故证据均为通过态截图。

## 2. 登录目标判定双分支观察（复用 A 的 TC-01、TC-02）

- 目标源码：`src/elmclient/src/views/Login.vue`，基线 `8efcb99`：
  第 82 行 `const user = response.data;`，**第 83 行 `if (!user) {`**，第 84 行 `alert('用户名或密码不正确！');`，
  第 85–88 行 else：`setSessionStorage('user', user); router.push({ path: '/index' });`。
- 观察方式：开发模式 dev-server（含源码映射）下，用 `branch_probe/observe_branch.py` 通过 CDP
  `Page.addScriptToEvaluateOnNewDocument` 注入只读探针（记录 XHR 响应、接管 alert 以避免原生弹窗阻塞），
  登录动作全部在真实登录页完成；在页面 JS 上下文按 axios 规则还原 `response.data` 并对 `!user` 求值。
  该方式与在 Sources 对第 83 行设断点读取局部变量 `user` 等价（探针求值点与判定同一数据）；未改业务源码。

| 复用用例 | 输入 | HTTP/响应（实测） | 还原后 user | `!user` 条件值 | 实际命中分支 | 页面效果（实测） | 证据 |
|---|---|---|---|---|---|---|---|
| TC-01 | 19112121212 / 123456Qaq | POST 8080 `/UserController/getUserByIdByPass`，**200**，响应体 8050 字节，首段 `{"userId":"19112121212",...,"userName":"JAVA爱好者"...}` | object，truthy | **false** | else：保存 user、路由跳 `/index` | URL=`http://localhost:8084/index`；`sessionStorage.user` 已写入；无 alert | evidence/branch/branch_observation.json（branch_false_correct_pwd）；branch_false_correct_pwd.png；branch_console.txt |
| TC-02 | 19112121212 / Wrong123 | 同接口 **200**，`Content-Length: 0` 空响应体（正常业务拒绝，**不是** catch/网络异常） | 空字符串 ""，falsy | **true** | if：提示用户名或密码不正确 | 停留在 `/login`；`sessionStorage.user=null`；捕获到 alert 参数「用户名或密码不正确！」 | evidence/branch/branch_observation.json（branch_true_wrong_pwd）；branch_true_wrong_pwd.png；branch_console.txt |

**局部判定覆盖：所选判定已覆盖分支数 = 2/2。**

明确不覆盖：登录函数其他空值校验、HTTP/网络异常 catch 分支、其他页面及后端分支；不声称整函数或全项目覆盖，
不称为 JUnit/JaCoCo 单元覆盖。这是源码驱动的一个判定（分支）覆盖实例。

## 3. 环境 / 可测性 / 自动化比例（并入总报告的小段落）

- **环境（S1 预检）**：前后端、MySQL 在一台演示机联通；前端 8084→后端 8080 跨域已放行；种子账号与目标商家/菜品数据核验存在。
  Python/Selenium 侧无系统级特殊依赖，Chrome 与 ChromeDriver 需大版本一致（本机均为 128）。
- **可测性**：登录、商家列表、商家详情均有稳定 URL 与可观察结果（路由跳转、sessionStorage、DOM 文本）；
  页面元素无 id/name，脚本按 placeholder、标签文本与 Vue 渲染出的 class 定位（如 `input[placeholder='手机号码']`、
  `ul.food li h3`），异步内容一律显式等待。数据可由后端接口与数据库直接核验，结果可观察、可复现。
- **自动化比例（两种口径分列，不混报）**：
  - 计划可自动化：TC-01～TC-09 的主要检查点可用脚本/工具验证，TC-10 人工布局判断不算全自动，**候选 9/10（估计，非已实现）**。
  - 本轮功能回归实际转脚本：**2/10 = 20%（TC-01、TC-03）**。
  - 若把 TC-09 的 JMeter 性能工具执行合并统计：**3/10 = 30%（含工具执行口径）**。
- **限制**：仅两轮 headless 重跑证据，不宣称长期稳定；浏览器/驱动升级需重新匹配；断言依赖种子数据不被改动。
